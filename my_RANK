--FIRST 3 ATEENDEES BY REGISTRATION DATE FROM EACH STATE --  

WITH RANKS AS ( 
SELECT *,
DENSE_RANK () OVER(PARTITION BY state ORDER BY [Registration Date] ASC) as DENSE_RANK_
FROM dbo.ConventionAttendees$
)

SELECT * FROM RANKS
WHERE DENSE_RANK_ <= 3
